[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs = .venv .git .claude deploy logs data
markers =
    macos_integration: safe real-macOS integration tests requiring osascript (requires PYTEST_MACOS_INTEGRATION=1; live Messages delivery is excluded)
