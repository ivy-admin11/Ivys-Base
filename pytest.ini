[pytest]
testpaths = tests
python_files = test_*.py
norecursedirs = .venv .git .claude deploy logs data
markers =
    macos_integration: macOS Messages integration tests that require osascript and Messages.app (requires PYTEST_MACOS_INTEGRATION=1)
