# Ivy Local Admin API Environment

See [CLAUDE.md](CLAUDE.md) for the architecture, development guidelines, and job
execution system. It is the single source of truth for agent instructions in
this repo.

For operational runbooks (running the engine, failover testing, health and
readiness checks, execution history, rendering launchd plists), see the
`ivy-ops` skill at `.claude/skills/ivy-ops/SKILL.md`.
